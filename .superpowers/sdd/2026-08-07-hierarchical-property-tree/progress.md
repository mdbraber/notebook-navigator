# SDD ledger — plan: .superpowers/design-docs/2026-08-07-hierarchical-property-tree.md

Branch: property-notes (main checkout, no worktree: tasks 4-5 deploy to ~/2027 from this tree)
BASE at start: a1d0b43f9dcd6a7163637b69c6bfed3c19b96089
Pre-flight scan: clean, no plan conflicts.

Task 1: complete (commits a1d0b43..3ca9ae8, review clean)
Task 1: minor (deferred): collect() comment claims it matches getTotalNoteCount's mechanism, but it dedups per DFS path not per call, so a diamond recomputes once per incoming path. Correctness unaffected.
Task 1: minor (deferred): avoidable `as PropertyTreeNodeId` cast at propertyHierarchy.ts:138, fixable by typing nodesById as Map<string, PropertyTreeNode>.
Task 1: minor (deferred): no test asserts the flag-off path returns EMPTY_PROPERTY_HIERARCHY_INDEX by reference, only that its maps are empty.

Task 2: implemented (commits 3ca9ae8..7cbddc5), review: Spec OK, quality Approved, 1 Important.
Task 2: Important finding: inlined comparator type instead of importing canonical PropertyNodeComparator.
Task 2: plan-conflict escalated to user. Plan text said import it from navigationComparators.ts, but src/utils imports from src/hooks zero times, so that would invert layering. User ruling: move PropertyNodeComparator to src/types/storage.ts, re-export from navigationComparators.ts, import in treeFlattener from ../types/storage.
Task 2: fix round 1/5 dispatched (resumed original implementer aa8101cd49a6697f4), awaiting result.
Task 2: minor (deferred): doc comment says it "differs only in" two ways from flattenTagTree, but it also takes a params object and returns an object; both mandated by the brief.
Task 2: minor (deferred): no test exercises the getChildComparator override path; inherited from the brief's test list.
Task 2: fix round 1/5 (1 addressed, 0 open; commits 7cbddc5..dd6b68d)
Task 2: complete (commits 3ca9ae8..dd6b68d, review clean)
Task 2: minor (deferred): treeFlattener.ts:22 mixes value and inline type imports in one specifier list; pre-existing style in that file.

Task 3: implemented (commits dd6b68d..43be81d), review: Spec OK, quality NOT approved, 2 Important.
Task 3: Important 1: propertyHierarchicalKeys not pruned in cleanupWithValidators, though existingPropertyKeys at PropertyMetadataService.ts:342 is already the right key format and prunes vaultProfiles[].propertyKeys at :356. Consequence: a deleted or renamed property keeps its flag, and key reuse silently inherits hierarchical status.
Task 3: Important 2: no tests for setPropertyHierarchicalKey/removePropertyHierarchicalKey/getPropertyHierarchicalKey, though tests/services/PropertyMetadataService.test.ts:287 has an analogous block for sort overrides. The delete-vs-false invariant is untested.
Task 3: minor (deferred): src/i18n/locales/fa.ts:2327 leaves "Vault" in Latin script while that file uses خزانه elsewhere.
Task 3: fix round 1/5 dispatched (resumed implementer a75059e4ed8b7ed2d).
Task 3: fix round 1/5 (2 addressed, 0 open; commits 43be81d..802d011d)
Task 3: complete (commits dd6b68d..802d011d, review clean). Suite now 2154.
Task 3: NOT a defect: propertyHierarchicalKeys absent from SYNC_MODE_SETTING_IDS is intentional and was an explicit constraint. Re-reviewer raised it as an observation only.

Task 4: implemented (commits 802d011..9eea190), review: Spec OK, quality Approved, 2 Important.
Task 4: Important 1: index memo deps [app, hierarchicalPropertyKeys, renderPropertyTree] cannot see link-resolution changes, though resolveValueNotePath calls getFirstLinkpathDest. Precedent PropertyTreeItem.tsx:129-133 carries vaultChangeVersion for exactly this. Fix: add sourceState.fileChangeVersion. NOTE: this dep list was prescribed by my brief AND resolutions, so it is a plan defect. It also deviates from the spec sentence "memoise on tree identity and the hierarchical key set alone"; I judged the spec's intent (stay out of the settings-save memo) preserved, and the cost is ~24 cheap lookups per file change, so I did not escalate.
Task 4: Important 2: no test pins the hierarchical emitter branch. tests/hooks/useNavigationPaneTreeSections.test.ts:324-329 already renders this hook and asserts propertyItems keys; a hierarchical case is ~30 lines there.
Task 4: minor (deferred): hierarchicalPropertyKeys uses Object.keys, but the service reader requires === true and sanitizeRecord preserves false, so a hand-edited {"projects": false} renders hierarchical with the checkmark off. One-word fix to filter on === true.
Task 4: minor (deferred, CARRY TO TASK 5): hasChildren is raw childIds.length > 0, ignoring the flattener's depth cap and cycle filter, so a placement at the cap or whose only child is its own ancestor gets a chevron that expands to nothing. Masked by the Task 5 expansion gap; becomes visible when Task 5 lands.
Task 4: minor (deferred): useNavigationPaneShortcutDisplay.ts:230 and fileItemPillDecoration.ts:105 still call getTotalPropertyNoteCount, so a hierarchical value's shortcut row and list pill disagree with its tree badge.
Task 4: minor (deferred): index memo is not gated on section visibility, so it rebuilds while the properties section is collapsed.
Task 4: report wording defect: claimed the vault tree "Matched exactly" while its own concerns note the depth-3 row is absent. Reviewer confirmed the absence is exactly the deferred Task 5 gap, not a wrong expansion set.
Task 4: fix round 1/5 dispatched (resumed implementer a4e61a670f9891f47).
Task 5 SCOPE DECISION (user): make collapseOtherBranchesOnExpand work per placement, deriving ancestors from placement chain prefixes rather than node ids. Chosen over skipping it for hierarchical keys. This reworks toggleNavigationExpansionTarget's contract, which folders and tags also use, so it gets its own task and review gate.
Task 5 research findings (7 touch points, my plan named 2):
  - NavigationPaneContent.tsx:1096 isExpanded uses item.data.id, must use item.key for PROPERTY_VALUE
  - NavigationPaneTreeRow.tsx:259 onToggle passes propertyNode.id, must pass item.key
  - useNavigationPaneKeyboard.ts:233 children.size > 0, always false for hierarchical values
  - useNavigationPaneTreeInteractions.ts:344, :640, :722 same children.size problem
  - handlePropertyToggle:331-360 collapseOtherBranchesOnExpand resolves a node from the id and passes getPropertyAncestorNodeIds
Task 4: fix round 1/5 (2 addressed, 0 open; commits 9eea190..cec8d18)
Task 4: complete (commits 802d011..cec8d18, review clean). Suite now 2156.
Task 4: minor (deferred): the frequency-comparator branch cannot be exercised by any current fixture. DEFAULT_SETTINGS.propertySortOrder is alpha-asc, so createFrequencyComparator returns undefined without calling getFrequency; and both sibling lists in the new test have 1 item, so Array.sort never invokes a comparator. Needs a frequency order plus 2+ siblings.
PLAN AMENDMENT: Task 5 split into two tasks so the collapse-others rework gets its own review gate, per the user's scope decision and the Task 4 reviewer's advice.
  Task 5 = expansion identity by placement (isExpanded, onToggle, the 4 children.size sites) + auto-reveal to first placement. For hierarchical keys the collapseOtherBranchesOnExpand path falls back to the plain dispatch, temporarily.
  Task 6 = collapseOtherBranchesOnExpand per placement, deriving ancestors from chain prefixes.

Task 5: implemented OUTSIDE the SDD loop (uncommitted working tree, no report, no implementer agent to resume). Reviewed as-is at 2166 passing / tsc clean / prettier clean.
Task 5: review verdict Spec FAILED, quality NOT approved. 3 Critical, 2 Important, 3 Minor.
Task 5: CRITICAL 1: nested placement keys are purged by CLEANUP_DELETED_PROPERTIES. NavigationPaneContent.tsx:513-520 whitelists key+value node ids; filterExpandedSet (ExpansionContext.tsx:64-84) is a whitelist, and every nested key contains a NUL. Effect deps include expandedProperties.size, so expanding a level-2 row purges itself on the next render. Persisted nested keys are purged at startup. MY RESOLUTIONS reasoned about root-placement persistence and missed non-root.
Task 5: CRITICAL 2: reveal can only expand ancestors that are already expanded. firstPlacementByNodeId is populated only for placements the flattener emitted (treeFlattener.ts:544) and it recurses only into expanded placements (:548), so a node id is in the map exactly when no expansion is needed. MY RESOLUTIONS prescribed this approach; the prescription is wrong. Needs a parent walk over the index, independent of expansion state.
Task 5: CRITICAL 3: navigateToProperty (propertyNavigation.ts:170-171) dropped the needs-expansion guard the tag precedent has (tagNavigation.ts:115-124). (a) with collapseOtherBranchesOnExpand on, revealing a nested value collapses its key, because SET_EXPANDED_PROPERTIES replaces the whole set and keyNodeId is omitted when the key is already expanded. (b) unconditional dispatch feeds a re-render loop with a localStorage write per iteration.
Task 5: Important 1: the keyboard children-presence fix changes behaviour in zero cases, because navigationExpansion.ts:244 returns hasChildren from item.data.children.size, always false for value nodes. Its comment claims otherwise and is wrong.
Task 5: Important 2: click paths half-migrated. :656 still reads isExpanded by node id and :681/:749 toggle by node id, so a nested click writes a bare node id into the persisted set and, for a promoted cycle root, toggles a different row than the one clicked.
Task 5: minor: 2 eslint errors are real TS2339 (item.level absent on RootSpacerItem). tsconfig.json includes only src/**, so npx tsc never sees test files, and the brief's eslint command did not cover tests/. The brief's verification steps could not have caught it.
Task 5: minor: useTagNavigation.ts:94-118 calls navigateToProperty without firstPlacementByNodeId (the property-pill reveal path).
Task 5: fix round 1/5: no original implementer exists to resume, so dispatching a fresh implementer per the skill's fallback.
Task 5: fix round 1/5 (6 addressed: 3 Critical, 2 Important, 1 Minor; commits cec8d18..a72647c8). Suite 2181.
Task 5: NEW CRITICAL from re-review: with collapseOtherBranchesOnExpand on, a root hierarchical value cannot be expanded by mouse at all. useNavigationPaneTreeInteractions.ts:370 builds the target with hasChildren: targetNode.children.size > 0, always false for value nodes, so canExpand is false and nothing dispatches. Reviewer verified by execution: zero dispatches. Fix: use propertyNodeHasChildren(targetNode, propertyHierarchyIndex). Also: the round's own test at tests/hooks/useNavigationPaneTreeInteractions.test.ts:833 claims to cover this but passes a KEY node, which does have children, so it proves nothing.
Task 5: NEW IMPORTANT from re-review: handlePropertyToggleAllSiblings (:833-844) still toggles by node id, so Alt+click on a nested placement toggles a different row and writes a bare node id into the persisted set. Verified by execution.
Task 5: minor (deferred): resolvePropertyRevealChain can return a chain whose head is not a root when a cycle member is reachable from a real root, so reveal expands a placement that renders nowhere. Silent no-op. Reviewer reproduced it. Also a chain longer than propertyHierarchyMaxDepth expands prefixes the flattener never recurses into.
Task 5: minor (deferred): useNavigationPaneKeyboard.ts:241-243 is uncovered; the required test reproduces the call sequence rather than driving the hook, so reverting the placement-key line breaks nothing.
Task 5: minor (deferred): left-arrow on a nested placement jumps to the key row rather than the parent placement (useNavigationPaneKeyboard.ts:541-548).
Task 5: minor (deferred, CARRY TO TASK 6): firstPlacementByNodeId is now dead. Reviewer notes property rows are indexed for scroll by node id (navigationIndex.ts:79-81), so its retained justification does not hold. Delete in Task 6.
Task 5: NOTE FOR TASK 6: the reviewer identified a narrower alternative to supportsBranchCollapse, deriving ancestors locally as [keyNodeId, ...getPropertyPlacementAncestorKeys(item.key.split(SEP))]. That is per-placement collapse-others, i.e. Task 6's job. Task 6 should also collapse the duplicated guard (supportsBranchCollapse vs placementKey === nodeId) into one helper; the two have already diverged, which is what produced the new Critical.
Task 5: fix round 2/5 dispatched.
Task 5: fix round 2/5: fixed the new Critical (propertyNodeHasChildren at :370) and the new Important (Alt+click placement key), commits a72647c8..bbb27a8a. Suite 2183. Implementer verified both new tests fail against a72647c8 and are the only two failures there.
Task 5: NOTE FOR TASK 6 (from the implementer): the Critical was a second instance of round 1's Important root cause, a children.size check on a node whose children map is empty by design. Two such sites were caught by review rather than by construction. Worth removing the need for callers of toggleNavigationExpansionTarget to supply children presence at all, since each caller must remember to ask the index.
Task 5: fix round 2/5 (2 addressed, 0 open; commits a72647c8..bbb27a8a). Re-reviewer independently reproduced the pre-fix failure state and confirmed exactly 2 failures.
Task 5: complete (commits cec8d18..bbb27a8a, review clean). Suite 2183.

PLAN NOTE: Task 6 agent was killed mid-task by a weekly API limit. Controller finished requirement 3's deletion and mechanical test fixes inline (UNREVIEWED controller work: removed 2 orphaned firstPlacementByNodeId refs, removed 2 tests asserting on it, adapted 6 flattener tests to the new array return shape). Agent then resumed after the limit reset and completed the rest.
Task 6: implemented (commits bbb27a8a..9a7e1950), 2186 passing, DONE_WITH_CONCERNS.
Task 6: vault note count went 157 -> 158. Investigated: Clippings/Why Is Everyone In Tech So Sad.md is a genuine Obsidian Web Clipper capture (27KB of article text, Noema source URL, iframe, author wikilink), created 05:42:46 during the agent window but not by it. Agent correctly did not delete it.
CORRECTION to an earlier claim: that new clipping carries tags: ["clippings"] even though the vault's Templates/Capture/Clipping Template.md had its tags key removed. So Web Clipper templates live in the browser extension, not the vault, and editing the vault template does not affect future clips. My earlier statement that removing it there stops new clippings getting tags was wrong.
Task 6: review Spec OK, quality Approved, 0 Critical, 0 Important, 4 Minor. Reviewer independently re-verified the 9 call sites / 8 construction sites of toggleNavigationExpansionTarget and confirmed only the 2 property sites changed, and that supportsBranchCollapse's !== false test was a tautology for every other type. Controller work verified complete and correct.
Task 6: CORRECTION to my own record: I described deleting "two tests"; it was one test (records the first placement of each node id) plus one assertion. The surrounding test kept its meaning.
Task 6: minor (deferred): task-6-report.md:131 says seven construction sites; the table below it correctly lists eight.
Task 6: minor (deferred): placement-key format knowledge now spans treeFlattener.ts (build/ancestors) and navigationExpansion.ts:232 (the split), and propertyNavigation.ts:172 independently composes the same set for reveal. Three expressions of "what must be expanded for a placement to render". No drift today.
Task 6: minor (deferred): the two rewritten tests dropped their negative assertions; a toHaveBeenCalledTimes(1) would guard against a future double dispatch.
Task 6: minor (deferred): the virtual-folder branch early return is pinned by inspection only, not a unit test.
Task 6: complete (commits bbb27a8a..9a7e1950, review clean). Suite 2186.
ALL SIX TASKS COMPLETE. Proceeding to the final whole-branch review.

FINAL WHOLE-BRANCH REVIEW: NOT merge-ready. 0 Critical, 4 Important, 4 Minor. Two Importants confirmed by the reviewer through execution.
FINAL: fix before merge (3): (1) hasChildren raw childIds.length ignores the flattener depth cap and cycle filter, so a chevron expands to nothing and with collapse-others on it trades away open branches; fix with a placement-aware helper. (2) resolvePropertyRevealChain can return a head that is not a root, so reveal can HIDE its target by branch-replacing away the branch it was visible in; walk up to a node in rootIds and stop at maxDepth. (3) hierarchicalPropertyKeys should filter === true, one word.
FINAL: USER DECISION on Important 1 (badge 16 vs list 5 for Fiddle): implement subtree selection, honouring the brainstorming choice. The user's vault has includeDescendantNotes: true AND projects hierarchical, so the incoherence is visible today, not hidden by defaults. Also bring the shortcut row and file pill onto the same count so all four surfaces agree.
FINAL: Important 4 (follow-up): useNavigationActions.ts:426-435 expand-all stops at a hierarchical key, and :241-248 smart collapse loses a selected nested value. Not in the ledger before; the branch made a previously complete assumption incomplete.
FINAL: rulings to record: the spec's internal placement cap was never built and is judged unreachable, so drop it; depth-cap truncation is silent because the flattener runs during render, so amend the spec rather than log; auto-reveal is a parent walk rather than "first placement", forced by Task 5 Critical 2.
FINAL: process note: tsconfig.json covers only src/**, so tsc never type-checks tests. npm run lint does cover tests/** via tsconfig.eslint.json; use that as the gate.
FINAL: fix wave dispatched (one dispatch, complete findings list).
FINAL: fix wave complete (commit 9a7e1950..b8ba443e). Suite 2196 (was 2186). All 4 items fixed including the user-decided subtree selection. Vault verified: Fiddle badge 5*11=16 and list 16; Building software 5/5; descendants-off 5/5. Each of 7 new tests verified to fail against the specific pre-fix code it covers.
FINAL: reveal chain signature chosen: resolvePropertyRevealChain({ index, keyNodeId, nodeId, maxDepth }), both new fields required because the two defects were exactly "did not know the roots" and "did not know the cap". Env field became one optional propertyHierarchy: { index, maxDepth } so an index cannot be supplied without the cap.
FINAL: concern: getTotalPropertyNoteCount now has no src/ callers, only tests/propertyTreeBuilder.test.ts. Left in place.
FINAL: concern: pill rainbow COLOURS still derive from a flat row list, so a nested value's pill colour can differ from its tree row. Counts agree. Out of scope.
FINAL: vault note count is now 159, up from 158, from a second genuine Web Clipper capture born 05:49:31 before this session's first command. Nothing created or deleted by any agent.
FINAL: scoped re-review of the fix wave: all 4 ADDRESSED. Reviewer verified each new test by reverting the production code four separate ways. Confirmed no tree mutation, no quadratic behaviour, and zero added cost for users who never enable the feature.
FINAL: one new Important from the re-review: baseFiles memo in useListPaneData.ts did not depend on propertyHierarchicalKeys, so toggling Hierarchical updated the badge while the list stayed stale. The transition case of the exact disagreement fix 4 removed.
FINAL: ADJUDICATION: the skill allows no second fix wave, so this would normally surface at the merge decision. The user said go and the fix is one dep-array entry, so I applied it as a CONTROLLER FIX (commit e7d9f387), unreviewed by an independent gate. tsc clean, eslint clean on the file, 2196 passing. The reviewer's suggested accompanying test (toggle the flag, assert badge and list agree) was NOT added; that seam remains uncovered.
FINAL: residual minors parked with rulings: getTotalPropertyNoteCount now has zero src/ callers and should be deleted or renamed (reviewer prefers deletion; its misnaming already caused the frequency-sort defect). Latent divergence between the chevron counting child ids and the flattener resolving nodes, unreachable while both derive from one render pass. The spec's internal placement cap was never built; ruled unreachable and dropped. Depth-cap truncation is silent; spec to be amended rather than logged. Auto-reveal is a parent walk, not "first placement"; spec amended in effect.
FINAL: reviewer struck one earlier ledger concern as unreachable: the scoped-index mismatch cannot occur for a property selection, since isScopedPropertyContextActive is false unless the selection is a folder or tag.
FINAL: 12 commits on the plan. Suite 2143 -> 2196. Proceeding to finishing-a-development-branch.

CAVEAT PASS (post-merge tidy of the four outstanding caveats). Suite 2196 -> 2203.
CAVEAT 1: the toggle-transition seam is now covered. tests/hooks/useListPaneData.test.ts renders useListPaneData for a hierarchical value selection and flips settings.propertyHierarchicalKeys, asserting the badge (createPropertyNoteCountInfo) and the list (result.files.length) agree in both directions: 2/2 flat, 4/4 hierarchical.
CAVEAT 1: HARNESS NOTE. The repo has no jsdom, no testing-library and no react-test-renderer, so there was no way to re-render a hook with its memo caches intact. The test uses a render-phase state update inside renderToStaticMarkup: React re-invokes the same component on the same fiber, so useMemo dependency comparison is real. Verified empirically before writing the test. Without this, a second renderToStaticMarkup call is a fresh instance and every memo rebuilds, which would make the dependency array unobservable.
CAVEAT 1: verified by deleting settings.propertyHierarchicalKeys from the baseFiles dep array: both tests fail, badge 4 vs list 2 marking hierarchical and badge 2 vs list 4 unmarking. Exactly the disagreement e7d9f387 removed.
CAVEAT 2a: getTotalPropertyNoteCount deleted from src/utils/propertyTree.ts. Its five test assertions now go through a local ownNoteCountForValue helper in tests/propertyTreeBuilder.test.ts reading keyNode.children.get(id)?.notesWithValue.size ?? 0, preserving each expected value (1, 2, 1, 0, 0). The design doc's two references to it are annotated rather than removed, since both describe the pre-feature code.
CAVEAT 2b: index threaded into useNavigationActions as a ref, not a value. Prop threading via navigationTreeSections works for NavigationToolbar and NavigationPaneHeader (both descend from NavigationPaneContent, which has props.navigationTreeSections) but NOT for NotebookNavigatorComponent:542, which calls the hook at line 542 while navigationTreeSections is computed at :1482. That file already carries propertyHierarchyIndexRef for exactly this ordering (useNavigatorReveal). So the hook takes { propertyHierarchyIndexRef }, NotebookNavigatorComponent passes the ref it already has, and NavigationPaneContent derives one from props.navigationTreeSections.propertyHierarchyIndex during render and forwards it to the toolbar and through NavigationPaneLayout to the header. One shape for all three entry points, so they cannot disagree.
CAVEAT 2b: expand all. The inline collectExpandablePropertyNodeIds closure became an exported pure helper collectExpandablePropertyExpansionKeys(propertyTree, hierarchy). A key with no rootIds entry (every non-hierarchical key, and every key when the index is empty) takes the original walk unchanged; a hierarchical key adds its key node id plus collectExpandablePropertyPlacementKeys.
CAVEAT 2b: the enumerator lives in treeFlattener.ts, not in the hook. The brief named isPropertyPlacementAtDepthCap and resolveRenderablePropertyChildIds as exports; they are private. Rather than widen them, the enumeration itself is exported, so the depth cap and the cycle filter keep one expression each. This is the third place placement-key knowledge would otherwise have spread to (see the Task 6 minor).
CAVEAT 2b: smart collapse. getSelectedPropertyKeyNodeId (returning one id) became buildSelectedPropertyParentKeys (returning the key node id plus the selected value's ancestor placement keys), and buildCollapsedExpansionState's selectedPropertyKeyNodeId became selectedPropertyParentKeys: Iterable<string>, matching selectedFolderParentPaths and selectedTagParentPaths. The chain comes from resolvePropertyRevealChain, the same resolver auto-reveal uses, so both agree on which placement of a multi-parent value to keep open. Two existing tests updated for the renamed field.
CAVEAT 2b: NEW BOUND. Placement enumeration is the one bulk path that walks placements without a user expanding each row, so the dropped placement cap's "unreachable" reasoning does not cover it. MAX_EXPANDABLE_PROPERTY_PLACEMENTS = 1000 bounds the walk and the persisted set. Measured worst case in the vault is 27 placements. Silent, like the depth cap. Recorded in the spec.
CAVEAT 2b: each new test verified to fail against the defect it covers. Reverting both helper bodies to the pre-fix logic fails exactly the 3 behaviour tests and leaves the 2 non-hierarchical guard tests passing; separately, removing only the depth-cap check from the enumerator fails only the cap test.
CAVEAT 3: three spec amendments. (1) Settings section: the placement cap was dropped, not built, with the per-placement-expansion reasoning, plus the new expand-all bound. (2) Edge-case table: depth-cap truncation is silent by design, because the flattener runs during render. (3) Decisions table and Auto-reveal section: reveal resolves a rooted chain by walking parentIds. Two consequential fixes: the flattener's documented return value no longer promises firstPlacementByNodeId, and the test-list bullet asserting that map is stable was replaced.
CAVEAT PASS vault verification: 160 markdown files outside .obsidian and .trash before and after, nothing created or deleted. The brief said 159; the extra is Clippings/Taste Is All That's Left.md, a genuine Web Clipper capture born 2026-08-08 07:08:08 with 16KB of notashelf.dev article text, a source URL and the same [[Source]] and [[Clippings]] frontmatter as the earlier two. That is the third such arrival during this feature's work.
CAVEAT PASS vault verification: from an empty property expansion set with a folder selected, Collapse / expand all now writes key:projects, key:projects=fiddle, key:projects=work and key:projects=work<NUL>key:projects=clients. Those four keys render the whole projects tree at levels 0 to 3, so no key names a row that renders nowhere. The other seven keys are the non-hierarchical property keys, unchanged.
CAVEAT PASS vault verification: with the depth-3 value Datawerkplaats Mooi Maasvallei selected, collapse all keeps exactly its ancestor chain (key:projects, key:projects=work, key:projects=work<NUL>key:projects=clients) and the selected row is still rendered at level 3, while Fiddle's subtree collapses. dev:errors clean across three plugin reloads.
CAVEAT PASS caveat: the navigation pane is virtualised, so a DOM row list is only the viewport. The full projects nesting was confirmed by narrowing the persisted set to the projects keys and reloading, not by reading one long DOM dump.
CAVEAT PASS residual: pressing Collapse / expand all during verification also changed folder and tag expansion, which was not snapshotted first. The property expansion set was restored exactly; folder and tag expansion are left as the two button presses left them. No setting in data.json was changed.
CAVEAT PASS lint: 312 problems across src/ and tests/ before and after, none in any touched file. tsc clean; tsc still does not cover tests/, so tests/ was linted explicitly.

CAVEATS PASS (commits 72ab443f..1c89cff7): all four closed. Suite 2196 -> 2203.
CAVEATS: introduced NEW production code not in the brief: MAX_EXPANDABLE_PROPERTY_PLACEMENTS = 1000 in treeFlattener.ts. Reasoning accepted as sound: placement enumeration for expand-all is the one bulk path that walks placements without a user expanding rows, so the dropped placement cap's "unreachable" argument no longer covers it.
CAVEATS: threaded the index into useNavigationActions as a REF, not a value, because NotebookNavigatorComponent calls that hook at :542 while navigationTreeSections appears at :1482. Precedent: the same file already carries propertyHierarchyIndexRef.
CAVEATS: caveat 1's test uses a novel harness for this repo, a render-phase state update inside renderToStaticMarkup, because there is no jsdom, testing-library or react-test-renderer available.
CAVEATS: UNRESOLVED SIDE EFFECT: vault verification pressed Collapse/expand all, which also changed folder and tag expansion in the live vault. Property expansion was restored exactly; folder and tag expansion was not snapshotted first and is left as two button presses left it. No data.json setting changed.
CAVEATS: vault count now 160, a third genuine Web Clipper capture born 07:08:08 before this session. Nothing created or deleted by any agent.
CAVEATS: review gate dispatched, since this pass added production code and a novel test pattern without one.
CAVEATS review: Spec OK, Approved, 0 Critical, 0 Important, 5 Minor. Reviewer verified by execution: a 1554-placement layered fixture proved enumeration output is a subset of rendered keys at maxDepth 1/2/3/10 including at the 1000 truncation boundary, under cycles and multi-parent values, with prefix closure preserved. It also probed the render-phase setState harness and confirmed useMemo dependency comparison is genuinely live.
CAVEATS review: confirmed folders and tags unaffected: only the property branch of buildCollapsedExpansionState changed, the folder and tag collectors are untouched, and the old truthy check became a loop over an iterable that is empty in exactly the old null cases.
CAVEATS review: confirmed nothing could persist a bad folder or tag expansion state. Both branches write sets built from live structure, persistence is localStorage UI state not data.json, and the diff does not touch those branches. The disclosed side effect is only that the user's folders and tags sit in a different but valid arrangement with the prior one recorded nowhere.
CAVEATS review minors, being closed now: (1) useNavigationActions.ts:389 comment claims the ref is not read during render, but shouldCollapseItems runs in render bodies at NavigationPaneHeader.tsx:182,197 and NavigationToolbar.tsx:82,94; fix the comment, not the code. (2) the new bound's "silent for the same reason the depth cap is" does not transfer, since it is reached in a click handler not during render. (3) MAX_EXPANDABLE_PROPERTY_PLACEMENTS has no test; deleting the check keeps all 2203 green. (4) the spec's PropertyHierarchyIndex block omits parentIds while two amended sections now reference it. (5) the ref plumbing through four components is pinned by nothing but tsc.

CAVEATS review minors closed: (1) comment rewritten to say the ref is read during render by the toolbar icon and aria-label as well as by the handler, that NavigationPaneContent writes it at the top of its own render so children see the current value, and that a React.memo skip on an index-only change leaves a reader's icon and aria-label one cycle stale. (2) picked logging over a rewritten justification: MAX_EXPANDABLE_PROPERTY_PLACEMENTS now logs a single console.debug when it truncates, since expand all is a command handler that runs once per click, unlike the depth cap which is hit during render; treeFlattener.ts and the design doc updated to say so, matching this codebase's existing habit of calling console.* directly rather than through a wrapper. (3) collectExpandablePropertyPlacementKeys gained tests/utils/treeFlattener.test.ts coverage: a 5 layer, 6 wide fully-connected fixture yields 1554 candidate placements (6 + 6^2 + 6^3 + 6^4), asserting the output is capped at exactly MAX_EXPANDABLE_PROPERTY_PLACEMENTS, is identical across two calls, and that every emitted key's parent prefix is also emitted. Verified by deleting the length check: the cap assertion fails with 1554 instead of 1000, the other two still pass. (4) design doc's PropertyHierarchyIndex block now lists parentIds with a doc comment matching propertyHierarchy.ts.

CAVEATS review minor 5, residual recorded rather than fixed: the hierarchy ref threaded through useNavigationActions, NavigationPaneContent, NavigationPaneHeader and NavigationToolbar is pinned by nothing but tsc. A future caller passing EMPTY_PROPERTY_HIERARCHY_INDEX instead of the live ref would keep all tests green, since there is no jsdom, testing-library or react-test-renderer in this repo to assert on rendered icon or aria-label output. Known gap, not closed here. Related observation from the same review: shouldCollapseItems() now runs resolvePropertyRevealChain, a breadth-first walk of index.parentIds, on every invocation, and it is invoked twice per render in each of NavigationPaneHeader and NavigationToolbar (once for the icon, once for the aria-label), where the pre-hierarchy code was O(1) string comparison. Bounded by one key's value count and not a problem in any measured vault, but worth knowing if that walk ever needs to run somewhere hotter.

MECHANICAL RESIDUALS PASS (post merge, at HEAD 850fc0cb, no runtime behaviour change intended). Ten deferred/reported items closed or dispositioned below. Suite 2217 -> 2219.

RESIDUAL 1 (doc comment overclaiming getTotalNoteCount's mechanism): closed by inspection, no edit needed. The collectSubtreeNotePaths comment in propertyHierarchy.ts no longer mentions getTotalNoteCount at all, and the walk itself now uses one visited Set for the whole call (an iterative stack, not a per path recursion guard), so the mechanism actually matches getTotalNoteCount's call wide dedup. Both the wording and the code already moved past the original Task 1 deferral, most likely during the FINAL fix wave's subtree selection rewrite (commit b8ba443e), which replaced the original collect() the deferral was about.

RESIDUAL 2 (avoidable `as PropertyTreeNodeId` cast): closed by inspection, no edit needed. Grepped the whole repo for the cast and for PropertyTreeNodeId in propertyHierarchy.ts: neither appears. Both nodesById construction sites (the subtree walk and collectPropertyValueSubtreeNotePaths) are already typed as `Map<string, PropertyTreeNode>` with no cast, which is exactly what this item asked for. Same likely origin as Residual 1: superseded by the FINAL rewrite.

RESIDUAL 3 (dead getChildComparator parameter): closed. Grepped every call of flattenPropertyHierarchy (one production site in useNavigationPaneTreeSections.ts, two test sites) and confirmed none passes getChildComparator; the per-key sort override is folded into createChildComparator, called once per key and reused for every level. Removed the parameter from FlattenPropertyHierarchyParams, its destructuring, and its use at the call site (now always `children.sort(comparator)`). src/utils/treeFlattener.ts.

RESIDUAL 4 (untranslated "Vault" in fa.ts): closed. propertyHierarchyMaxDepth.desc at fa.ts:2327 now reads "خزانه‌ها" instead of "Vaultها", matching the plural used at fa.ts:282. Left the three unrelated pre-existing Latin-script "Vault" occurrences elsewhere in the file (lines ~949/955/961, startup notices) untouched: out of scope for this item and not part of this feature's strings.

RESIDUAL 5 (frequency comparator has no reachable test): closed. Added a test to tests/hooks/useNavigationPaneTreeSections.test.ts: a hierarchical "projects" key with propertySortOrder: frequency-asc and two root siblings, Fiddle (own count 1, one child contributing 3 more notes, subtree count 4) and Solo (own count 2, no children, subtree count 2). Own-count ascending order is Fiddle, Solo; subtree-count ascending order is Solo, Fiddle. Verified by temporarily changing the comparator's getFrequency to `node.notesWithValue.size` (own count): the new test fails with the Fiddle-then-Solo order, confirming it actually exercises subtreeCount. Reverted the temporary change before committing.

RESIDUAL 6 (report miscount): closed. task-6-report.md:130-131 now says "eight construction sites in total", matching the eight row table immediately below. Number only, no other text touched.

RESIDUAL 7 (placement-key parsing spread across files): closed. Added `parsePropertyPlacementKey(placementKey): string[]` beside buildPropertyPlacementKey in src/utils/treeFlattener.ts. Routed navigationExpansion.ts:232 through it. In ExpansionContext.tsx, isExpandedPropertyEntryValid's two step check (an `includes` short circuit plus a `split().every()`) collapsed to one `parsePropertyPlacementKey(entry).every(...)` call: for an entry with no separator, split returns a one element array, so `.every` reduces to the exact same plain membership test the `includes` branch produced, no behaviour change. PROPERTY_PLACEMENT_SEPARATOR stays exported: treeFlattener.ts still uses it internally (propertyPlacementHasRenderableChildren) and tests/utils/treeFlattener.test.ts imports it directly to test that same module, so both remaining uses are legitimate and unexporting it would break that test's import.

RESIDUAL 8 (missing double dispatch guards): closed. Added `expect(<spy>).toHaveBeenCalledTimes(1)` after the existing toHaveBeenCalledWith assertion in: tests/utils/navigationExpansion.test.ts's "replaces the expanded set with the whole placement chain for a nested placement" (spy named `dispatch`), and all three collapseOtherBranchesOnExpand cases under `describe('handlePropertyToggle placement keys', ...)` in tests/hooks/useNavigationPaneTreeInteractions.test.ts (spy named `expansionDispatch`): "branch replaces a nested placement by mouse...", "still takes the collapse-others branch for a key node", and "expands a root hierarchical value through the collapse-others branch...".

RESIDUAL 9 (keyboard expansion test does not drive useNavigationPaneKeyboard): left open, reported rather than forced. Read tests/hooks/useListPaneData.test.ts's render-phase harness first, then worked out what driving useNavigationPaneKeyboard for real would require: useKeyboardNavigation attaches its keydown listener via a useEffect calling containerRef.current.addEventListener, and renderToStaticMarkup runs neither effects nor produces a real DOM, so the hook's only interaction surface (a real keydown event) is unreachable without jsdom. A harness that mocks useKeyboardNavigation to capture its internal onKeyDown callback and calls it directly is technically possible, but tracing the two sites where the local placementKey variable (lines 241-243) is actually read, propertyPlacementHasChildren's precheck and the expandedProperties.has(placementKey) precheck, shows both are independently re-verified downstream by toggleNavigationExpansionTarget using item.key directly (the real, correct placement key, unaffected by the local variable), and the depth-cap divergence the precheck could introduce only ever makes the precheck MORE permissive than correct, never less, so it is always masked by the flattener-supplied hasChildren the caller already carries. This is a rigorous version of what the ledger's Task 5 minor already said ("reverting breaks nothing"): there is no dispatched action, and no other externally observable effect, that distinguishes the correct line from a revert. A test that could only assert on the internal call arguments to propertyPlacementHasChildren, not on anything a caller could observe, is exactly the kind of test that passes for an incidental reason the brief warned against, so left as a documented gap rather than written.

RESIDUAL 10 (hierarchy ref into useNavigationActions pinned only by tsc): closed. Unlike useNavigationPaneKeyboard, useNavigationActions returns plain callback functions with no event/DOM dependency, so it can be rendered directly: mocked ExpansionContext, SelectionContext, ServicesContext, SettingsContext, UXPreferencesContext and StorageContext in tests/hooks/useNavigationActions.test.ts (the hook's only unmocked dependencies), rendered the real hook via renderToStaticMarkup with a propertyHierarchyIndexRef, called the returned handleExpandCollapseAll, and asserted the dispatched SET_EXPANDED_PROPERTIES set. With the real hierarchical index the set includes the nested placement key; with EMPTY_PROPERTY_HIERARCHY_INDEX in the ref it collapses to just the key node id. Verified by temporarily hardcoding EMPTY_PROPERTY_HIERARCHY_INDEX inside readPropertyHierarchy in the real hook: the new test fails exactly as expected (loses both the root and the nested placement key), then reverted.

MECHANICAL RESIDUALS PASS verification: full suite 2219 passing (2217 baseline + the Residual 5 and Residual 10 tests). `npx tsc -noEmit -skipLibCheck` clean. `eslint` clean on every touched src and tests file (tests linted explicitly, since tsconfig.json covers only src/**). `prettier --write` run on every touched src and tests file, none needed reformatting except the new imports in useNavigationActions.test.ts, which prettier itself reformatted. .superpowers/sdd/.gitignore was not touched by any tool in this pass.

PLACEMENT-AWARE NAVIGATION PASS (from HEAD 1e6c1f83). Four approved behaviour gaps, all four closed. Suite 2219 -> 2236. Full report: .superpowers/design-docs/2026-08-08-placement-aware-navigation.md.

ITEM 1 (navigation row index keyed property rows by node id): closed. buildNavigationPathIndexMap now stores item.key for both PROPERTY_KEY and PROPERTY_VALUE rows. Verified item.key === item.data.id for every row a non-hierarchical key emits (useNavigationPaneTreeSections.ts:934 key rows, :971 flat value rows) and for a root placement (treeFlattener.ts, one-element chain), so folders, tags, virtual folders, shortcuts, key rows and every non-hierarchical value are byte for byte unchanged. Audited all thirteen getNavigationIndex/setNavigationIndex/map call sites plus normalizeNavigationPath's five callers; the table is in the design doc.

ITEM 1 FINDING, reported rather than converted: no property lookup in the repo has a placement to pass. Selection is a node id end to end (SelectionState.selectedProperty is PropertySelectionNodeId, persisted that way, the row highlight compares selectedProperty === item.data.id, shortcuts and the public API address node ids), so useNavigationPaneKeyboard's getCurrentIndex, useNavigationPaneScroll's selected-path resolve and NavigationPaneContent's getSelectedRenderedItem - which feeds triggerSelectedItemCollapse, the very command item 1 is about - all have only a node id. A bare re-key would have made every one of them return -1, the silent stop the brief warned about. Kept a node id FALLBACK entry instead: written in a second pass after every row is in the map and never overwriting, so a real placement key always wins (necessary for a promoted cycle member, which is both a root and somebody's child, propertyHierarchy.ts:169). No node-id-to-placement conversion was invented. Net behaviour change: a bare node id resolves to the topmost placement instead of the bottom-most, deterministic instead of arbitrary, and only for values that render more than once. Removing the ambiguity outright needs selection to carry a placement: persistence format, highlight comparison, shortcuts and public API, out of scope here.

ITEM 2 (left-arrow on a nested placement jumped to the key row): closed. New pure helper getPropertyPlacementParentKey(placementKey) in treeFlattener.ts returns the chain minus its last segment, or null for a one-element chain. useNavigationPaneKeyboard uses `getPropertyPlacementParentKey(item.key) ?? buildPropertyKeyNodeId(propertyNode.key)`, so a root placement and every flat value still fall back to the key row exactly as before.

ITEM 3 (Alt+click could not expand a hierarchical subtree): closed by reusing collectExpandablePropertyPlacementKeys, not by a second walk, so the depth cap, the cycle filter and MAX_EXPANDABLE_PROPERTY_PLACEMENTS all apply and no unrenderable placement is emitted. A value row takes the enumeration filtered by chain prefix (strictly below the clicked placement); a key row takes all of it; a key that is not hierarchical keeps getAllDescendantPropertyNodeIds untouched. Also fixed the hierarchical KEY row, which was broken the other way: it emitted the key's value node ids, and a non-root value's node id names no row, which is the same junk-expansion-key defect class this project has already fixed twice.

ITEM 4 (property pill reveal did not nest): closed. The index was already published to PropertyTreeService every render (NotebookNavigatorComponent.tsx:1500), so the plumbing existed and only lacked a reader: added PropertyTreeService.getHierarchyIndex(), and useTagNavigation now passes propertyHierarchy from it plus settings.propertyHierarchyMaxDepth, the same shape useNavigatorReveal passes. No service or no published index yet means the empty index, which expands the key alone as before.

VERIFICATION: 9 of the 17 new assertions fail against pre-fix src (verified by `git stash push -- src/`: 4 files failed, 9 tests failed, 53 passed) - 4 for item 1, 2 for item 2, 2 for item 3, 1 for item 4. Also added a folder-and-tag regression pin, a non-hierarchical-value pin, a root-placement pin, a virtual-collection pin, a depth-cap pin and two pill fallback pins. Full suite 2236 passing, tsc clean, prettier and eslint clean on all ten touched files (tests linted explicitly). .superpowers/sdd/.gitignore untouched.

KNOWN GAP carried forward, same as Residual 9: item 2's three lines of glue inside useNavigationPaneKeyboard are pinned by tsc only. useKeyboardNavigation attaches its keydown listener in a useEffect on a real DOM node and this repo has no jsdom, testing-library or react-test-renderer, so the hook cannot be driven; the derivation (getPropertyPlacementParentKey) and the lookup it feeds (buildNavigationPathIndexMap) are both tested, and the wiring was verified by hand in the live vault instead.

VAULT: 163 markdown files outside .obsidian and .trash, before and after. Last recorded 161; investigated with find -newermt and the difference is the user's own activity earlier on 2026-08-08 (five Web Clipper captures, Test Power Bases.md, Test.md, _types/task.md), nothing created or deleted by this pass. Confirmed by real CDP input in the live vault: left-arrow from Work > Clients collapses it then selects the Work placement at level 1 (pre-fix: the key row at level 0); Alt+click on Work expands Work plus the Work > Clients placement and renders the level-3 row (pre-fix: a plain toggle); the Building software pill reveal expands key:projects plus the key:projects=fiddle ancestor placement and selects the nested row (pre-fix: the key alone). Folders re-checked unchanged (click Templates/Bases, ArrowLeft selects Templates, ArrowLeft collapses it). Tags could not be smoke-tested: no tag rows render in this vault at all, so the tag side rests on the unit test.

VAULT NOTE: categories is NOT marked hierarchical in the live vault, only projects, so the multi-parent Clients/Software values named in the brief do not nest there. The Work > Clients chain used for verification is a projects chain (Clients.md carries projects: [[Work]]).

VAULT RESTORE: enablePropertyInternalLinks was toggled off to reach the pill reveal path at all, since a wikilink pill opens its link while that setting is on, and restored to true afterwards via plugin.saveSettingsAndUpdate(). Every snapshotted localStorage value was written back, selected-property removed, and Test.md re-opened as the active file before the final reload, because a reload auto-reveals the active file's property and would otherwise have left a different selection. Final state matches the snapshot exactly: expanded-properties ["key:projects=work<NUL>key:projects=clients","key:projects=work"], expanded-folders ["Templates"], expanded-virtual-folders [], expanded-tags [], selected-property null, selected-folder "/", selected-file "Test.md". No data.json setting left changed. dev:errors clean.

OUT OF SCOPE, untouched as instructed: buildFileItemPropertyRainbowColors (fileItemPillDecoration.ts:244). The pill side still emits a flat item list while the navigation pane emits nested placements, so the rainbow positions differ; matching them needs the pill side to know expansion state, which is an open design question for the user.
